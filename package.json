{
  "name": "agent-sandbox-workflow",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun main.ts",
    "typecheck": "bunx tsc --noEmit",
    "format": "bunx biome check --write main.ts index.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.6",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.72"
  }
}
